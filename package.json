{
  "name": "mediscan-ai",
  "version": "1.0.0",
  "private": true,
  "description": "MediScan AI full-stack starter (React + Express + OpenAI integration).",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "echo Run server and client in separate terminals using npm run dev:server and npm run dev:client"
  }
}
